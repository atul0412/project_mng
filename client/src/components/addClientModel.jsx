import { useState } from 'react';
import { useMutation } from '@apollo/client';
import { FaUser } from 'react-icons/fa';

export default function addClientModel() {
    return (
        <div>AddClientModel `</div>
    )
}
